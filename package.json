{
  "name": "darpanberi.github.io",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "prepare": "husky install",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint:eslint": "eslint --ext .js .",
    "lint:style": "stylelint \"assets/css/**/*.css\"",
    "lint:markdown": "markdownlint \"**/*.md\" --ignore node_modules --fix",
    "lint": "npm run lint:eslint && npm run lint:style && npm run lint:markdown",
    "test": "jest",
    "test:coverage": "jest --coverage",
    "serve": "http-server -p 8080 -c-1 .",
    "check-all": "npm run format:check && npm run lint && npm run test:coverage"
  },
  "devDependencies": {
    "@testing-library/dom": "^9.3.1",
    "eslint": "^8.57.0",
    "eslint-plugin-import": "^2.29.1",
    "eslint-plugin-promise": "^6.1.1",
    "http-server": "^14.1.1",
    "husky": "^9.0.11",
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0",
    "lint-staged": "^15.2.8",
    "markdownlint": "^0.31.1",
    "markdownlint-cli": "^0.39.0",
    "prettier": "^3.2.5",
    "stylelint": "^16.6.1",
    "stylelint-config-standard": "^36.0.0"
  },
  "lint-staged": {
    "**/*.{js,json,md}": [
      "prettier --write"
    ],
    "assets/css/**/*.css": [
      "prettier --write"
    ],
    "**/*.js": [
      "eslint --fix"
    ]
  }
}

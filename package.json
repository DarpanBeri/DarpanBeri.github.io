{
  "name": "darpanberi.github.io",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "prepare": "husky",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint:eslint": "eslint .",
    "lint:style": "stylelint \"assets/css/**/*.css\"",
    "lint:markdown": "markdownlint \"**/*.md\" --ignore node_modules --fix",
    "lint": "npm run lint:eslint && npm run lint:style && npm run lint:markdown",
    "test": "jest",
    "test:coverage": "jest --coverage",
    "serve": "http-server -p 8080 -c-1 .",
    "validate:html": "html-validate --formatter stylish index.html",
    "check:links": "start-server-and-test 'npm run serve' http://localhost:8080 'linkinator http://localhost:8080 --recurse --skip \"^mailto:,^tel:,^javascript:,linkedin\\\\.com\"'",
    "check:a11y": "start-server-and-test 'npm run serve' http://localhost:8080 'axe http://localhost:8080 --exit'",
    "test:e2e": "playwright test",
    "test:e2e:debug": "playwright test --headed",
    "check-all": "npm run format:check && npm run lint && npm run test:coverage && npm run test:e2e && npm run validate:html && npm run check:links"
  },
  "devDependencies": {
    "@axe-core/cli": "^4.7.2",
    "@eslint/js": "^9.0.0",
    "@html-validate/stylish": "^4.3.0",
    "@playwright/test": "^1.56.1",
    "baseline-browser-mapping": "^2.9.19",
    "eslint": "^9.0.0",
    "eslint-plugin-promise": "^7.0.0",
    "globals": "^15.0.0",
    "html-validate": "^8.29.0",
    "http-server": "^14.1.1",
    "husky": "^9.0.11",
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0",
    "linkinator": "^7.5.0",
    "lint-staged": "^15.2.8",
    "markdownlint": "^0.40.0",
    "markdownlint-cli": "^0.47.0",
    "prettier": "^3.2.5",
    "start-server-and-test": "^2.1.2",
    "stylelint": "^16.6.1",
    "stylelint-config-standard": "^36.0.0"
  },
  "lint-staged": {
    "**/*.{js,json,md}": [
      "prettier --write"
    ],
    "assets/css/**/*.css": [
      "prettier --write"
    ],
    "**/*.js": [
      "eslint --fix"
    ]
  },
  "overrides": {
    "markdownlint-cli": {
      "glob": ">=11.1.0"
    }
  }
}
